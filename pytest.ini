[pytest]
markers =
    unit: fast tests with no external services or model downloads
    integration: tests that require an external service such as Neo4j
    e2e: end-to-end pipeline contract tests
    ml: model/runtime smoke tests that may install or load ML dependencies
    destructive: migration/backfill tests that mutate isolated fixture data
