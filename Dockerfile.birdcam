FROM python:3.11-slim
WORKDIR /app
COPY . .
RUN pip install --no-cache-dir fastapi uvicorn PyYAML opencv-python-headless numpy neo4j ultralytics
ENV PYTHONPATH=/app
CMD ["python", "-m", "birdcam.cli", "api", "--config", "/config/config.yaml"]
