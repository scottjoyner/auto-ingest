# Birdcam integration module

This module is a **separate integration** for auto-ingest. It does not replace core platform services.

Use `Dockerfile.birdcam` and `docker-compose.birdcam.yml` for birdcam-only deployment.
